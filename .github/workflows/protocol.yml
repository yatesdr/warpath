name: Protocol

on:
  push:
    paths:
      - 'protocol/**'
  pull_request:
    paths:
      - 'protocol/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build
        working-directory: protocol
        run: go build ./...

      - name: Test
        working-directory: protocol
        run: go test ./... -v
